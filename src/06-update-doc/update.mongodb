use("platzi_store")

db.products.updateOne(
  // query
  { _id: 4 },
  // change => operators
  {
    $set: {
      name: "change name",
      price: 3000,
      tags: ["A", "B", "C" ]
    }
  }
)

db.products.find()
